<Window x:Class="BlenderToolbox.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Width="1220"
        Height="820"
        MinWidth="920"
        MinHeight="700"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextForegroundBrush}">
    <Grid Margin="16"
          Background="{DynamicResource WindowBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="14" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{DynamicResource SidebarBackgroundBrush}"
                BorderBrush="{DynamicResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="{DynamicResource CornerRadiusShell}"
                Padding="18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Style="{StaticResource SidebarTitleTextStyle}"
                               Text="Giga Papl" />
                    <TextBlock Margin="0,8,0,0"
                               Style="{StaticResource HelperTextStyle}"
                               Text="Compact desktop tools for render, file, and batch tasks."
                               />
                </StackPanel>

                <TextBlock Grid.Row="1"
                           Margin="0,18,0,10"
                           Style="{StaticResource SidebarCaptionTextStyle}"
                           Text="TOOLS" />

                <ListBox Grid.Row="2"
                         MinHeight="80"
                         ItemsSource="{Binding Tools}"
                         SelectedItem="{Binding SelectedTool}"
                         ItemContainerStyle="{StaticResource SidebarToolItemStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock
                                       FontWeight="SemiBold"
                                       Text="{Binding DisplayName}"
                                       TextWrapping="Wrap" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <TextBlock Grid.Row="3"
                           Margin="0,12,0,0"
                           Style="{StaticResource HelperTextStyle}"
                           Text="Local Blender workflow tools."
                           />
            </Grid>
        </Border>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="14" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Style="{StaticResource PanelCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource ScreenTitleTextStyle}"
                               Text="{Binding SelectedTool.DisplayName}" />
                    <TextBlock Margin="0,8,0,0"
                               Style="{StaticResource HelperTextStyle}"
                               Text="{Binding SelectedTool.Description}"
                               />
                </StackPanel>
            </Border>

            <ContentControl Grid.Row="2"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Content="{Binding SelectedTool.View}" />
        </Grid>
    </Grid>
</Window>
